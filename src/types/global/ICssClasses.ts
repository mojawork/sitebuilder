export enum EThemes {
    light = 'theme-light',
    dark = 'theme-dark'
}

export enum ELayoutMod {
    mEmpty = '',
    mZoom = 'm-zoom',
}


export enum EDirections {
    left = 'left',
    center = 'center',
    right = 'right',
    split = 'split'
}