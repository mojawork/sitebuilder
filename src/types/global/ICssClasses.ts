export enum EThemes {
    light = "theme-light",
    dark = "theme-dark",
    customize = "theme-customize"
}

export enum ELayoutMod {
    mEmpty = "",
    mZoom = "m-zoom"
}

export enum ETextColors {
    success = "text-success-g",
    error = "text-error-g",
    warning = "text-warning-g",
    focus = "text-focus-g"
}

export enum EDirections {
    left = "left",
    center = "center",
    right = "right",
    split = "split"
}
